import React from 'react';
import FilterPanel from '../filterPanel/FilterPanel';

const Home = () => {
    return (
        <div>
            <FilterPanel />
        </div>
    );
};

export default Home;
