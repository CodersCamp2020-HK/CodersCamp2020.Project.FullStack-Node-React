import React from 'react';
import FilterPanel from '../components/filterPanel/FilterPanel';

const Home = () => {
    return (
        <div>
            <FilterPanel />
        </div>
    );
};

export default Home;
